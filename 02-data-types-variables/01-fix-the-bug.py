# This code raises an error. Fix it!  
a = "10"  
b = 20  
# print(a + b) # It is because a number can't be added to a string
# To solve the problem we need to convert the string to a number
print(int(a) + b)